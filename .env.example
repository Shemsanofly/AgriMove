# Africa's Talking Sandbox — https://account.africastalking.com
AT_USERNAME=sandbox
AT_API_KEY=atsk_your_sandbox_api_key_here

# Optional: short code / sender ID (sandbox uses default if empty)
AT_SENDER_ID=

# Public HTTPS URL when testing USSD on Africa's Talking simulator (not needed for SMS)
PUBLIC_BASE_URL=
